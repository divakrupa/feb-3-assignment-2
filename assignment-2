#include <stdio.h>
long int fact(int) ;
int main() 
{
   int n, f;
    printf("enter n value");
    scanf("%d",&n);
    f=fact(n);
    printf("the factorial is %d",f);
    return 0;
}
long int fact(int n)
{
    if(n==1)
    return 1;
    else
    return n*fact(n-1);
}
